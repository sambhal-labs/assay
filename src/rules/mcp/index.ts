import type { Rule } from '../../core/types.js';

// Populated by the MCP rule modules (protocol, definition, tokens, security,
// reliability) — see the allocation table in ../index.ts.
export const mcpRules: Rule[] = [];
