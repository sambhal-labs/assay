// Commander wiring lands with the vertical slice (Phase 2); this stub keeps
// the build entry valid until then.
import { PACKAGE_NAME, TOOL_NAME, TOOL_VERSION } from './constants.js';

console.log(`${TOOL_NAME} v${TOOL_VERSION} (${PACKAGE_NAME}) — CLI wiring in progress`);
